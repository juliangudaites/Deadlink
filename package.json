{
  "name": "deadlink",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DEADLINK — Send it once. Then it's gone.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "node index.js",
    "start:full": "npm run build --prefix client && node index.js",
    "deploy:check": "npm run build --prefix client && node --check server/src/index.js"
  }
}